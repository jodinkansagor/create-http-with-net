const request = require('supertest');
const app = require('../lib/app');

describe('createResponse', () => {
  it('gives us the correct response when given type and path', () => {
    const request = { method: 'GET', path: '/' };

    
  })
});
